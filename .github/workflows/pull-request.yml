name: Pull Request
on:
  pull_request:
    branches:
      - main
jobs:
  test:
    uses: './.github/workflows/test.yml'
  make:
    needs: test
    secrets: inherit
    uses: './.github/workflows/make.yml'
